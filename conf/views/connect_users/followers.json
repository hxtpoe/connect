{
    "name": "followers",
    "views": {
        "all": {
            "map": "function(doc, meta) { if(doc.t === 'fp') { emit(doc.followeeId + '_' + doc.followerId)} }"
        },
        "count": {
            "map": "function(doc, meta) { if(doc.t === 'fp') { emit(doc.followeeId)} }"
        }
    }
}