{
    "name": "tweets",
    "views": {
        "allTweets": {
            "map": "function(doc, meta) { if(doc.t === 'tweet') { emit('tweet_' + doc.author + '_' + doc.timestamp , meta.id);  } }"
        }
    }
}